<style>
    body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
    }

    .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        border: 1px solid #ddd;
        border-radius: 8px;
    }

    h2 {
        color: #555;
    }

    .btn {
        display: inline-block;
        padding: 10px 20px;
        background-color: #007bff;
        color: #fff;
        text-decoration: none;
        border-radius: 5px;
    }

    .btn:hover {
        background-color: #0056b3;
    }

    .table {
        width: 100%;
        margin-top: 20px;
        border-collapse: collapse;
    }

    .table th,
    .table td {
        border: 1px solid #ddd;
        padding: 8px;
    }

    .table th {
        background-color: #f2f2f2;
        text-align: left;
    }
</style>

<div class="container">
    <h2>Invoice</h2>
    <p>Dear <%= user.fullName %>,</p>
    <p>Thank you for your order. Below are the details of your invoice:</p>

    <table class="table">
        <tr>
            <th>Song Link</th>
            <td><a href="<%= songLink %>" target="_blank">
                    <%= songLink %>
                </a></td>
        </tr>
        <tr>
            <th>No. of Streams</th>
            <td>
                <%= noOfStreams %>
            </td>
        </tr>
        <tr>
            <th>Country to Chart In</th>
            <td>
                <%= country %>
            </td>
        </tr>
        <tr>
            <th>Similar Artists</th>
            <td>
                <%= similarArtists %>
            </td>
        </tr>
        <tr>
            <th>Total Cost</th>
            <td>$<%= cost %>
            </td>
        </tr>
        <tr>
            <th>Issue Date</th>
            <td>
                <%= issueDate %>
            </td>
        </tr>
        <tr>
            <th>Due Date</th>
            <td>
                <%= dueDate %>
            </td>
        </tr>
        <tr>
            <th>Status</th>
            <td>
                <%= status==='paid' ? 'Paid' : 'Unpaid' %>
            </td>
        </tr>
    </table>

    <p>To make the payment, please click the link below:</p>
    <p><a href="<%= url.resolve(sails.config.custom.baseUrl, `/store/invoice/${id}`) %>" class="btn">Make Payment</a>
    </p>

    <p>Thank you for your business.</p>
    <p>Sincerely,</p>
    <p>Audiobaze Store.</p>
</div>